name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: Kombucha-Batch-Tracker/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: Kombucha-Batch-Tracker

      - name: Export static web build
        run: npx expo export --platform web
        working-directory: Kombucha-Batch-Tracker

      - name: Add .nojekyll
        run: touch Kombucha-Batch-Tracker/dist/.nojekyll

      - uses: actions/upload-pages-artifact@v3
        with:
          path: Kombucha-Batch-Tracker/dist

      - uses: actions/deploy-pages@v4
        id: deployment
