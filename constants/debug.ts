/**
 * DEBUG_MODE — enable for testing:
 * - Allows decimal day values (e.g. 0.1 days = ~2.4 hours) for timer testing
 * - Stepper uses 0.1 increments instead of 1
 * - Keyboard shows decimal pad
 */
export const DEBUG_MODE = true;
